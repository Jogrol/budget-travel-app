type Query {
  feed: [Post!]!
  expenses: [Expensive]
  drafts: [Post!]!
  post(id: ID!): Post
}

type Mutation {
  createDraft(title: String!, content: String): Post
  deletePost(id: ID!): Post
  publish(id: ID!): Post
  createExpense(description: String!, ammount: Float!): Expensive
}

type Post {
  id: ID!
  published: Boolean!
  title: String!
  content: String!
}

type Expensive {
  id: ID!
  description: String!
  ammount: Float!
}
